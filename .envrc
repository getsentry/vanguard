layout node

export DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vanguard?schema=public"

# load local environment variables
if [ -f ".env" ] ; then
  dotenv .env
else
  echo "Unable to find .env. Please see README for configuring environment."
fi
